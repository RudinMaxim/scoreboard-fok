FROM node:24-alpine AS runtime

WORKDIR /app

ENV NODE_ENV=production
ENV PORT=3000

EXPOSE 3000

CMD ["node", "-e", "console.log('client-control Dockerfile placeholder: add production control start command here')"]
